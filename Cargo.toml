[workspace]
members = [
    "crash-context",
    "crash-handler",
    "minidumper",
    "minidumper-test",
    "sadness-generator",
]

exclude = ["minidumper-test/crash-client"]

[profile.dev]
debug = 2
