[package]
name = "minidumper"
version = "0.1.0"
edition = "2021"

[dependencies]
# Nicer cfg handling
cfg-if = "1.0"
crash-context = { version = "0.1.0", path = "../crash-context" }
libc = "0.2"
# Basic log emitting
log = "0.4"
# Minidump writing (currently linux/android only)
minidump-writer = { git = "https://github.com/EmbarkStudios/minidump_writer_linux", branch = "macos" } # branch = "main"
#minidump-writer = { path = "../../mdw" }
# Event loop
polling = "2.2"
# Nicer locking primitives
parking_lot = "0.12"
# Nicer error creation
thiserror = "1.0"

[target.'cfg(any(target_os = "linux", target_os = "android"))'.dependencies]
# Improved Unix domain socket support, includes features that are not available in std
uds = "0.2.6"

[target.'cfg(any(target_os = "windows", target_os = "macos"))'.dependencies]
# Nicer binary interop
scroll = "0.11"

[target.'cfg(target_os = "windows")'.dependencies.windows-sys]
version = "0.34"
features = [
    "Win32_Foundation",         # Core types eg HANDLE
    "Win32_Networking_WinSock", # Sockets
    "Win32_System_IO",          # Sockets
    "Win32_System_Threading",   # OpenProcess
]

[dev-dependencies]
# Diskwrite example
crash-handler = { path = "../crash-handler" }
# uuid generation
uuid = { version = "1.0", features = ["v4"] }
backtrace = "0.3"
